"""Minimal harness for the AI Portfolio Bootcamp.

Replace the reference task in `schema.py` and `pipeline.py` with your own.
Keep the shape: config from env, structured logging, one `run(input) -> output`.
"""
